# Conflict resolution

Resolve conflicts in the smallest possible scope.

1. Update the default branch locally.
2. Rebase or merge it into the working branch.
3. Resolve each conflict by understanding both intended changes.
4. Run the relevant tests again.
5. Review the resulting diff before pushing.

Avoid choosing an entire side automatically when both sides contain valid work.
