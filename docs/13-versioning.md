# Versioning

Use a consistent versioning policy and document its compatibility promise.

- Increment the major version for intentional breaking changes.
- Increment the minor version for backward-compatible capabilities.
- Increment the patch version for backward-compatible fixes.
- Use prerelease identifiers for versions that are not production-ready.

Version numbers should describe compatibility, not the amount of work involved.
