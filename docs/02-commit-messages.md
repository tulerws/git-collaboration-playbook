# Commit messages

Write the subject as a short command that describes the resulting change.

- Prefer `Add retry limit` over `Added retry limit`.
- Keep unrelated changes in separate commits.
- Use the body to explain motivation or non-obvious tradeoffs.
- Reference an issue only when the relationship is useful.

Before committing, review the staged diff with `git diff --staged`.
